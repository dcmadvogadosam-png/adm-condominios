const btn=document.querySelector('.menu-btn'),menu=document.querySelector('.menu');btn.onclick=()=>{menu.classList.toggle('open');btn.textContent=menu.classList.contains('open')?'✕':'☰'};document.querySelectorAll('.menu a').forEach(a=>a.onclick=()=>{menu.classList.remove('open');btn.textContent='☰'});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.getElementById('year').textContent=new Date().getFullYear();
// PERSONALIZE AQUI: 55 + DDD + número, somente dígitos.
const CONTACT={whatsapp:'5592999999999',email:'contato@admcondominios.com'};const wa=document.getElementById('whatsappLink'),em=document.getElementById('emailLink');wa.href=`https://wa.me/${CONTACT.whatsapp}?text=${encodeURIComponent('Olá! Gostaria de solicitar uma proposta para administração de condomínio.')}`;em.href=`mailto:${CONTACT.email}`;em.textContent=CONTACT.email;
const form=document.getElementById('leadForm'),statusEl=document.getElementById('formStatus');form.onsubmit=async e=>{e.preventDefault();statusEl.textContent='Enviando...';try{const data=Object.fromEntries(new FormData(form).entries());const r=await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error);statusEl.textContent='✓ Solicitação enviada com sucesso!';form.reset()}catch(err){statusEl.textContent='Não foi possível enviar agora. Use o WhatsApp ou tente novamente.'}};
